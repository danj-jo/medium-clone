import {ReactElement} from "react";
import {FlatList, SafeAreaView, ScrollView, Text, View} from "react-native";
import { MaterialIcons } from '@expo/vector-icons';
const  HomeNav = (): React.ReactElement => {
   return (
       <>

       </>
   )
}

export default HomeNav
